# Copy to .env and fill in real values. Never commit .env.

# Anthropic API key (used in Phase 3 for LLM rationale generation)
ANTHROPIC_API_KEY=

# Database URL (defaults to local SQLite for development)
DATABASE_URL=sqlite:///./data/investment.db
